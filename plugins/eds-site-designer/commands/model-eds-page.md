---
name: model-eds-page
description: Turn an approved aesthetic direction into an EDS authoring and CSS ownership contract without implementing it
---

Load the `eds-site-designer` skill and run **LOCK**, then model **COMPILE** for: $@

## Inputs

Use the supplied design plate, aesthetic contract, reference, or clearly stated direction. If no direction exists, run DIRECT first; do not discover the aesthetic through an implementation plan.

## Required output

1. Locked aesthetic invariants with evidence and allowed adaptations.
2. Section/default-content/block inventory.
3. Concrete authored content contracts: author-facing input and location, accepted values/default, pre-decoration DOM, decorated DOM, variant/class mapping, invalid-value fallback, enhancement mode, optional states, and malformed-content behavior.
4. Existing-block versus new-block decisions with rationale.
5. Global/section/prose/block ownership ledger using `templates/eds-ownership-ledger.md` from the installed skill.
6. Wide/intermediate/narrow transformation model.
7. Sticky, semantics, progressive-enhancement, and blast-radius risks.
8. Browser and content-state validation plan.

Do not edit production files. Use a semantic HTML table artifact for a large ownership ledger; keep the chat summary concise.
